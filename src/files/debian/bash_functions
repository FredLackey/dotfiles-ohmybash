#!/bin/bash

# Debian-based Bash Functions
# Functions specific to Debian-based systems (Ubuntu, Raspberry Pi OS)

# Source common functions first
# Get the directory where this file actually lives (resolves symlinks)
BASH_FUNCTIONS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [ -f "${BASH_FUNCTIONS_DIR}/../common/bash_functions" ]; then
    source "${BASH_FUNCTIONS_DIR}/../common/bash_functions"
fi

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Debian-specific functions would go here
# Currently, all Debian-specific functions are further specialized
# in ubuntu/ and pios/ subdirectories

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
