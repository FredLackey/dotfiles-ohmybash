#!/bin/bash

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Source .bash_profile for all interactive shells
# This ensures both login and non-login shells get the same configuration

if [ -f "$HOME/.bash_profile" ]; then
    source "$HOME/.bash_profile"
fi
